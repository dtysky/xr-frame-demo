<xr-demo-viewer
  title="AR OSDMarker案例"
  intro="Marker识别案例，点击渲染区域可以上传OSDMarker。识别成功后，会于屏幕绘制一个纯色立方体展示位置。"
  code="{{xmlCode}}"
>
  <xr-ar-osdmarker
    disable-scroll
    id="main-frame"
    width="{{renderWidth}}"
    height="{{renderHeight}}"
    style="width:{{width}}px;height:{{height}}px;top:{{top}}px;left:{{left}}px;display:block;"
    markerImg="{{markerImg}}"
  />

  <button bindtap="handleChangeMarkerImg">
    点击替换Marker图像
  </button>
</xr-demo-viewer>